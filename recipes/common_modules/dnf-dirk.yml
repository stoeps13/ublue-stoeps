---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json
type: dnf
repos:
  cleanup: true
install:
  skip-unavailable: true
  packages:
    - acpi
    - task
    - tasksh
    - taskopen
